# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-16 画面の見せ方を整理した

**決めたこと**

- 升目の上に出る線は3つだけ。お手本・学生の筆跡（正解でも不正解でも黒）・ナビ
- 間違った画を赤くするのはやめた。どこが違ったかはナビが全画を走ることで示す
- お題の判定は「正解」「不正解」の2つだけ。画数は数えない（5画中5画正解は読めない）
- まとめ書きの合計は「正解 3/7」。1字だけ選んだときは合計を出さない
- 「一発正解」という言葉は画面に出さない。用語としては残す（数え方の決まりのため）
- 結果の一覧は升目にかぶせない。上の帯と升目のあいだに一列で出す

**次にやること**

- 利用者に試用版（https://claude.ai/artifact/MeAcARuzoEQ4s7pfSLCGTB）を触ってもらう
- 筆跡の記録（https://claude.ai/artifact/PFKJmXsRPopRkBXRdE6MD5）で実機の軌跡を集め、
  以後の判定調整はその固定データに対して回す
- 変更提案 #16 を `main` に取り込む（利用者の合図を待つ）
- 取り込み後、#18〜#22、#24〜#30 を閉じる。そのあと #9（語のデータ）から再開する

**未解決の問題**

- 長い画1本の字（一・し・く など31字）は、まっすぐ20ずらしても正解のままになる
- 判定の数値はいまも人工的に作ったブレで決めている。実機の筆跡は1本も無い
- 実機（スマホ）での目視確認は未了
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了

## 2026-09-15 テストを封印し、判定を升目の位置に戻した

**決めたこと**

- 書き方は1つだけにする。テストは画面からもコードからも外した（設計記録 0012）
- 表紙は言語を選んで START を押すだけ。点線・お手本・1画目からのナビは常に出る
- 「練習」「テスト」という言葉は用語集からも消した
- 字は升目の位置で判定する。重ね直しはやめた（設計記録 0013、0010 は撤回）
- 画の端が許される距離の下限を 10 から 16 に上げた。まっすぐ12のずれまでは全字が正解
- 上限22は下げない。下げると「指で書いたような字」の合格率が87%→71%に落ちるため
- 書く画面の左に選んだ字を並べる案は、作らないことにした

**次にやること**

- 利用者に試用版（https://claude.ai/artifact/MeAcARuzoEQ4s7pfSLCGTB）を触ってもらう
- 筆跡の記録（https://claude.ai/artifact/PFKJmXsRPopRkBXRdE6MD5）で実機の軌跡を集め、
  以後の判定調整はその固定データに対して回す
- 変更提案 #16 を `main` に取り込む（利用者の合図を待つ）
- 取り込み後、#18〜#22、#24〜#30 を閉じる。そのあと #9（語のデータ）から再開する

**未解決の問題**

- 長い画1本の字（一・し・く など31字）は、まっすぐ20ずらしても正解のままになる
- 判定の数値はいまも人工的に作ったブレで決めている。実機の筆跡は1本も無い
- 実機（スマホ）での目視確認は未了
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了

## 2026-09-15 曲がっている画を定規で引いたら不正解にした

**決めたこと**

- 画ごとに「お手本の曲がりをどれだけ残したか」を見る。半分以上失ったら不正解
- 手のふるえは速い揺れなので、比べる前にならして消す。まっすぐな画には問わない
- 画がずれてよい量は、升目に対する固定値ではなく、その画自身の長さに対する割合にする
- 判定の数値はいまも人工的に作ったブレで決めている。実機の筆跡はまだ1本も無い（設計記録 0011）

**次にやること**

- 利用者に試用版で「わざとダメな字」を書いてもらい、不正解になるか確かめる
- 筆跡の記録（https://claude.ai/artifact/PFKJmXsRPopRkBXRdE6MD5）で実機の軌跡を集め、
  以後の判定調整はその固定データに対して回す
- 変更提案 #16 を `main` に取り込む（利用者の合図を待つ）
- 取り込み後、#18〜#22 と #24〜#27 を閉じる。そのあと #9（語のデータ）から再開する

**未解決の問題**

- 厳しくした代償で、指で書いたような字が合格する割合は 92% から 86% に下がった
- 「い」のはねを途中まで書いて残りを直線にした場合は、まだ合格してしまう
- 実機（スマホ）での目視確認は未了
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了

## 2026-09-14 まとめ書き（文字の複数選択）を作った

**決めたこと**

- 文字はタップで選び、選んだ順に番号が付く。「はじめる」で選んだ順に1字ずつ書く
- 上限なし。種類（ひらがな・カタカナ・漢字）をまたいで選べる。同じ字は1回だけ
- 1字書き終わるごとに「次へ／やり直す」。やり直しても数え方は最初の結果のまま
- 最後の字を書き終えると、その字に重ねて合計と字ごとの○×を出す
- テストは途中で結果を出さず、書き終えてからまとめて出す
- 「ぜんぶ」でいま見ている種類の全字を選び、もう一度押すと外れる
- ホームは表紙のまま。捨てるものがあるときだけ確認を出す（選んだ字／あと◯字）
- まとめ書きは語とは別物として扱う（設計記録 0008）
- かなは五十音表の形に並べる。表紙の絵は試用版でも出るようにした

**次にやること**

- 利用者に試用版でまとめ書きを触ってもらう
- 変更提案 #16 を `main` に取り込む（利用者の合図を待つ）
- 取り込み後、#18〜#22 と #16 を閉じる
- #9（語のデータ）から再開する。`/grill-with-docs` を通してから実装する

**未解決の問題**

- 実機（スマホ）での目視確認は未了。ブラウザでの確認までは済んでいる
- 判定が厳しすぎないかは未確認
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了

## 2026-09-14 ナビを画の半分で次へ進めた

**決めたこと**

- ナビは時計で判断しない。書いている画がお手本の半分の長さに達したら次の画を指す
- 最初の画は自力で書かせる。ただし一度まちがえたらその画を指す
- 画面側の毎フレームの時計は廃止し、指の動きに合わせて出し分ける

**次にやること**

- 利用者に試用版を触ってもらい、ナビの出る速さが合っているかを確認する
- 変更提案 #16 を取り込む（利用者の合図を待つ）
- #9（語のデータ）から再開する。`/grill-with-docs` を通してから実装する

**未解決の問題**

- ナビの速さが実機で合っているかは未確認
- 判定が厳しすぎないかは未確認
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了


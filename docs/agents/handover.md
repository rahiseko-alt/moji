# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-13 #5（升目に指で書ける）を実装し提出（/implement 3件目）

**決めたこと**

- 画面の切り替えは `src/app/app.ts` が持つ。出ていく画面は必ず `destroy()` する
- 升目は `src/writing/writing-surface.ts`。canvas 1枚に、点線の枠・薄いお手本・
  書いた線をすべて描く。判定は一切持たない（#6 の `writing-session` の役目）
- 座標はすべて KanjiVG の 109x109 の空間で扱う。画面の大きさに依存しない
- なぞった軌跡は時刻つきの点列 `TracedPoint` として保持し、`strokes()` で読める
- お手本は元のパス（`d`）を Path2D で描く。点列ではないのでなめらかに出る
- ホームの確認は全面を覆う板ではなく中央の札にする。何を捨てようとしているかが
  見えるため。既定の答えは安全側の「いいえ」
- 文字選択の画面（#8）が無いため、書く字は「日」に固定している

**次にやること**

- 利用者に GitHub の Settings > Pages で公開元を「GitHub Actions」に設定してもらう。
  これが済むまで公開URL（https://rahiseko-alt.github.io/moji/）は 404 のまま
- PR #1 を `main` に取り込む（下書き。取り込み可・自動チェック通過済み）
- 次は #6（採点と書き直し）。このアプリの中核。`writing-session` を切り出し、
  唯一の主たるテスト対象にする

**未解決の問題**

- #4 と #5 は `/code-review` にまだかけていない（#3 はかけて反映済み）
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了
- 【曖昧】用語集（`CONTEXT.md`）が無いまま用語を新造している。`/domain-modeling` で拾う
- 指で書いた線の採点精度は実機で試すまでわからない（#6）
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可

## 2026-09-13 #4（筆順データ）を実装し提出（/implement 2件目）

**決めたこと**

- 172字612画の筆順データを KanjiVG から生成し `assets/data/strokes.json` に同梱した。
  生成物はコミット済みで、組み立てにネットワークは要らない
- 1画につき2つ持つ。`d`（元のパス。お手本をなめらかに描く用）と
  `median`（等間隔16点。書いた線との照合用）
- KanjiVG 由来のデータはコードと別ファイルに分ける。CC BY-SA 3.0 の継承条件が
  アプリのコードに及ばないようにするため
- 漢字80字は文部科学省の一次情報と1字ずつ照合し完全一致を確認。別表PDFは文字が
  アウトライン化されていて抽出できないため「音訓の学校段階別割り振り表」から取った。
  全学年1026字が告示と一致することで抽出方法の正しさを裏づけた
- 表紙を開いた時点で筆順データの読み込みを始める（書く画面で待たせない）
- 表紙の隅に「出典」を置き、KanjiVG の指定文言どおりの帰属と配当表の出典を表示する
- 生成スクリプトは `npm run build:data`。KanjiVG の場所は引数か `KANJIVG_DIR` で渡す

**次にやること**

- 利用者に GitHub の Settings > Pages で公開元を「GitHub Actions」に設定してもらう。
  これが済むまで公開URL（https://rahiseko-alt.github.io/moji/）は 404 のまま
- PR #1 を `main` に取り込む（下書き。取り込み可・自動チェック通過済み）
- 次は #5（升目に指で書ける）。採点はまだ入れない

**未解決の問題**

- #4 は `/code-review` にまだかけていない（#3 はかけて指摘を反映済み）
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了
- 【曖昧】用語集（`CONTEXT.md`）が無いまま用語を新造している。`/domain-modeling` で拾う必要がある
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可

## 2026-09-13 #3（土台と表紙）を実装し提出（/implement 1件目）

**決めたこと**

- 作りは Vite + TypeScript の静的サイト。UIフレームワークは使わない。テストは Vitest
- 配色は `assets/cover.png` から実測した値を `src/styles/tokens.css` に置く。
  以降の画面はこのトークンだけを使い、色を直書きしない
- 表紙の絵は切り取らない。画面比が絵と違うぶんは和紙色の帯で埋め、ボタンは絵の座標系
  （`container-type: size` + `cqw`/`cqh`）に対して配置する
- 縦向きは `inert` で本体ごと到達不能にする。案内は4言語を並べ、選んだ言語を先頭に出す
- 変更提案の確認と GitHub Pages への配信は1つのワークフロー（`.github/workflows/ci.yml`）
  にまとめた。`deploy` ジョブは `main` のときだけ動く
- `/code-review` の指摘のうち、角印・富士山との重なりは4つの画面比で実測して否定した。
  それ以外（ワークフローの二重化、未使用コード、命名、選択状態の弱さ）は修正済み
- 「テストが1本もない」という指摘は違反ではない。自動確認は `writing-session` と
  生成データの2か所だけに絞る合意があり、そのどちらも #6 / #4 でこれから作る

**次にやること**

- 利用者に GitHub の Settings > Pages で公開元を「GitHub Actions」に設定してもらう。
  これが済むまで公開URL（https://rahiseko-alt.github.io/moji/）は 404 のまま
- PR #1 を `main` に取り込む（下書きのまま。取り込み可・自動チェック通過済み）
- 次は #4（筆順データの用意）。KanjiVG から かな92字・小1漢字80字の点列を生成する

**未解決の問題**

- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了
- 【曖昧】用語集（`CONTEXT.md`）が無いまま `Choices` / `orientation gate` 等の語を新造した。
  `/domain-modeling` で拾う必要がある
- 指で書いた線の採点精度は実機で試すまでわからない（#6）
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可

## 2026-09-13 MojiDrill を13の作業単位に分割（/to-tickets 完了）

**決めたこと**

- 仕様書 Issue #2 の子として、作業指示書 #3〜#15 を発行した（全件 `ready-for-agent`）
  1. #3 土台と表紙 / 2. #4 筆順データ / 3. #5 升目に指で書ける / 4. #6 採点と書き直し
  5. #7 ナビ / 6. #8 文字選択の画面 / 7. #9 語のデータ / 8. #10 語をひとまとまりで書ける
  9. #11 4言語 / 10. #12 読み上げ / 11. #13 テスト / 12. #14 選択漢字コース / 13. #15 オフライン
- 先に終わらせる順は各Issueの「Blocked by」に記載。#3 から着手できる。#15 は最後に回す
- #6（採点と書き直し）がこのアプリの中核。`writing-session` という状態機械に進行を切り出し、
  そこが唯一の主たるテスト対象になる
- 表紙の画像を利用者から受領し、`assets/cover.png`（1672x941、16:9）に置いた

**次にやること**

- `/implement` を実行し、#3（土台と表紙）から順に作る
- PR #1（引き継ぎメモの記録）が下書きのまま残っている。取り込むか閉じるかの判断が要る

**未解決の問題**

- 指で書いた線がどの程度の精度で採点できるかは実機で試すまでわからない。
  採点の4つの閾値は初期値を緩めに置き、1か所にまとめる
- ベトナム語・ネパール語の訳は下訳のみ。母語話者の確認が未了
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可

## 2026-09-13 MojiDrill の仕様書を発行（/to-spec 完了）

**決めたこと**

- 仕様書を Issue #2 として発行した（ラベル `ready-for-agent`）。利用者ストーリー60件、
  実装方針、テスト方針、対象外、補足を含む
- 自動で確認する箇所は2つだけに絞ることで合意した
  1. 書き取りの進行（`writing-session`）… 採点とナビ発動もこの中で確認する
  2. 生成した文字・語彙データ … 80字ある、全字に筆順がある、全語が4言語の訳を持つ、など
- 画面の見た目・なぞり心地・音の読み上げは自動確認しない。実機で目視する
- 作りは サーバーなしの静的サイト、TypeScript + Vite、UIフレームワークなし、
  描画は Canvas、テストは Vitest、オフラインは Service Worker、配信は GitHub Pages
- 採点は Hanzi Writer(MIT) の4条件（向き・始点終点・形・長さ）を参考に自前実装。
  閾値は1か所にまとめ、実機で調整できるようにする

**次にやること**

- `/to-tickets` を実行し、Issue #2 を作業単位に割る
- 表紙の画像ファイル（横長）をこの置き場所に直接アップロードしてもらう。
  会話に貼られた画像はファイルとして保存できない。届くまでは仮の背景で進める
- PR #1（引き継ぎメモの記録）が下書きのまま残っている。取り込むか閉じるかの判断が要る

**未解決の問題**

- 指で書いた線がどの程度の精度で採点できるかは実機で試すまでわからない。
  採点の4つの閾値は初期値を緩めに置く
- 【曖昧】学校名「AIビジネス専門学校」は名古屋市中村区に実在を確認したが、
  日本語学科の記載はなく使用教材も非公開。貴校が本当にこの学校かは未確認
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可


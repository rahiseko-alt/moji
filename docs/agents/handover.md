# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-13 #7（ナビ）を実装

**決めたこと**

- 2画目以降、1.5秒手が止まったら、次の画の始点から朱色の点が書く方向へ進む
- 通った道筋は薄く残す。向きは静止画では伝わらないため動かす
- 出す出さないの判断は進行の状態機械が持ち、画面は描くだけ
- 間違えて突き返された画のあとにも出す

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作）
- 実機で指で書き、採点の閾値とナビの待ち時間を調整する
- PR #1 を `main` に取り込む
- #8（文字選択の画面）に進む

**未解決の問題**

- 実機での確認が一度もできていない

## 2026-09-13 #6（採点と書き直し）を実装し、#4〜#6 の指摘を反映

**決めたこと**

- 1画ごとに正誤を判定する。向き・始点終点の距離・形・長さの4条件すべてを満たしたときだけ正解
- 不正解の理由は backwards / misplaced / shape / tooShort の4つ。画ごとに保持する
- 練習は間違った画を受け付けず同じ画を待つ。テストは間違っても次へ進み、お手本も出さない
- 得点は「1回目で正解した画の数」
- 進行の状態機械が、ナビの待ち時間・ナビを出すか・時刻を受け取るところまで持つ。描画は #7
- 採点の閾値は1か所にまとめた。実機で調整する
- 筆順データの生成はアプリと同じ並べ直し処理を使う。生成物は1バイトも変わらないことを確認済み
- 漢字80字の検査は、出典から独立に書き写した一覧と突き合わせる

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作）
- 実機で指で書き、採点の閾値を調整する
- PR #1 を `main` に取り込む
- #7（ナビの描画）に進む

**未解決の問題**

- 実機での確認が一度もできていない。ここまでの確認はすべてマウス操作による代替
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了
- 用語集（`CONTEXT.md`）と `docs/adr/` が無い。決めた理由の置き場所が無い
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可

## 2026-09-13 #5（升目に指で書ける）を実装

**決めたこと**

- 画面は1度に1つだけ表に出し、出ていく画面は必ず片づける
- 升目は描くことと拾うことだけを担い、正誤は一切知らない
- 座標はすべて文字自身の 109 四方の空間で扱う。画面の大きさに依存しない
- なぞった軌跡は時刻つきの点列として持ち、そのまま採点に渡す
- お手本は元のパスを描く。点列ではないのでなめらかに出る
- ホームの確認は中央の札にする。既定の答えは安全側の「いいえ」

**次にやること**

- #6（採点と書き直し）に進む

**未解決の問題**

- 文字選択の画面（#8）が無いため、書く字は「日」に固定している

## 2026-09-13 #4（筆順データ）を実装

**決めたこと**

- ひらがな46字・カタカナ46字・小学1年配当漢字80字、612画ぶんを同梱する
- 1画につき、元のパスと等間隔16点の両方を持つ
- 生成物はコミットする。組み立てにネットワークは要らない
- KanjiVG 由来のデータはアプリのコードと別ファイルに分ける
- 漢字80字は文部科学省の一次情報と1字ずつ照合し完全一致を確認した
- 表紙の隅から出典を開ける

**次にやること**

- #5（升目に指で書ける）に進む

**未解決の問題**

- なし

## 2026-09-13 #3（土台と表紙）を実装

**決めたこと**

- サーバーを持たない静的サイト。UIフレームワークは使わない
- 配色は表紙の絵から実測した値をトークンにまとめる。以降の画面で色を直書きしない
- 表紙の絵は切り取らない。ボタンは絵の座標系に対して置く
- 縦向きは本体ごと到達不能にする。案内は4言語を並べ、選んだ言語を先頭に出す
- 変更提案の確認と配信は1つのワークフローにまとめる

**次にやること**

- #4（筆順データ）に進む

**未解決の問題**

- 公開URLは、利用者が Settings > Pages を設定するまで 404 のまま


# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-13 実機で試して採点を調整し、不具合を3件直した

**決めたこと**

- 設定を待たずに実機で触れるよう、同じ中身を会話から開ける試用版として公開した。
  学生に配る正式な公開先とは別で、利用者だけが見られる
- 採点の閾値は勘で動かさない。いろいろな書き方が実際にどんな数値になるかを先に測り、
  正解にすべき書き方と不正解にすべき書き方の境目に置く
- その境目をテストに固定した。正解側（完璧・手ぶれ・強い手ぶれ・少し中心外れ）と
  不正解側（1割のずれ・45度の傾き・2割手前で終わる・曲線を直線で書く）の両方を検証する
- 突き返した画は列から外して別に持つ。位置で覚えていると、その間に画が増えたときに
  別の画を消してしまう
- 最初に触れた指以外は受け付けない。手のひらが触れると以降が無視されるため
- 書き終わったら入力そのものを止める。判定されない線が残らないようにするため

**次にやること**

- 利用者に試用版を再度触ってもらい、今度は厳しすぎないかを確認する
- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作。
  スマホではブラウザを「PC版表示」に切り替える必要がある）
- PR #1 を `main` に取り込む
- #9 以降は `/grill-with-docs` を通してから実装する

**未解決の問題**

- 判定が厳しすぎないかは未確認。緩める場合も境目のテストごと動かす
- ナビが実機で見えているかを確認できていない

## 2026-09-13 用語集と設計判断の記録を作った

**決めたこと**

- `CONTEXT.md` を作った。文字・画・語・升目・お手本・ナビ・書き取り・練習・テスト・
  一発正解・場面などの呼び方を1つに決め、避ける言い方も書いた
- `docs/adr/` に7件。教材ではなく告示から字種を取る／筆順データをコードと分ける／
  座標は文字自身の109四方／自動テストは2か所だけ／記録を一切残さない／
  UIフレームワークなし／ホームはどの画面でも表紙に戻る
- ホームの行き先を変えた。仕様書と食い違って見えるため ADR に経緯を残した

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作。
  スマホではブラウザを「PC版表示」に切り替えないと Settings が出ない）
- 実機で指で書き、採点の閾値とナビの待ち時間を調整する
- PR #1 を `main` に取り込む
- #9 以降は `/grill-with-docs` を通してから実装する

**未解決の問題**

- `CONTEXT.md` は「語（1〜4文字）」を書き取りの単位と定義しているが、
  コードはまだ1文字しか扱えない。#10 で揃う
- 実機での確認が一度もできていない

## 2026-09-13 #8（文字選択の画面）を実装

**決めたこと**

- 表紙 → 文字選択 → 書く画面の3画面が一本につながった
- 上部で ひらがな／カタカナ／漢字 を切り替える
- かなは五十音順、漢字は画数の少ない順。同じ画数なら五十音順で固定する
- 全部の字が一度に画面に収まるよう、枠の大きさを自動で縮める。スクロールさせない
- ホームは表紙ではなく文字選択の画面に戻る

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作）
- 実機で指で書き、採点の閾値とナビの待ち時間を調整する
- PR #1 を `main` に取り込む
- #9（語のデータ）に進む

**未解決の問題**

- 文字選択の画面から表紙へ戻る手段が無い。言語を選び直すには開き直すしかない。
  仕様どおりだが、利用者に確認が要る
- 実機での確認が一度もできていない

## 2026-09-13 #7（ナビ）を実装

**決めたこと**

- 2画目以降、1.5秒手が止まったら、次の画の始点から朱色の点が書く方向へ進む
- 通った道筋は薄く残す。向きは静止画では伝わらないため動かす
- 出す出さないの判断は進行の状態機械が持ち、画面は描くだけ
- 間違えて突き返された画のあとにも出す

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作）
- 実機で指で書き、採点の閾値とナビの待ち時間を調整する
- PR #1 を `main` に取り込む
- #8（文字選択の画面）に進む

**未解決の問題**

- 実機での確認が一度もできていない

## 2026-09-13 #6（採点と書き直し）を実装し、#4〜#6 の指摘を反映

**決めたこと**

- 1画ごとに正誤を判定する。向き・始点終点の距離・形・長さの4条件すべてを満たしたときだけ正解
- 不正解の理由は backwards / misplaced / shape / tooShort の4つ。画ごとに保持する
- 練習は間違った画を受け付けず同じ画を待つ。テストは間違っても次へ進み、お手本も出さない
- 得点は「1回目で正解した画の数」
- 進行の状態機械が、ナビの待ち時間・ナビを出すか・時刻を受け取るところまで持つ。描画は #7
- 採点の閾値は1か所にまとめた。実機で調整する
- 筆順データの生成はアプリと同じ並べ直し処理を使う。生成物は1バイトも変わらないことを確認済み
- 漢字80字の検査は、出典から独立に書き写した一覧と突き合わせる

**次にやること**

- GitHub の Settings > Pages で公開元を「GitHub Actions」に設定する（利用者の操作）
- 実機で指で書き、採点の閾値を調整する
- PR #1 を `main` に取り込む
- #7（ナビの描画）に進む

**未解決の問題**

- 実機での確認が一度もできていない。ここまでの確認はすべてマウス操作による代替
- ベトナム語・ネパール語の文言は下訳。母語話者の確認が未了
- 用語集（`CONTEXT.md`）と `docs/adr/` が無い。決めた理由の置き場所が無い
- 不要な枝が残っている。枝の削除は安全装置に阻まれ AI 側で実行できない。放置可


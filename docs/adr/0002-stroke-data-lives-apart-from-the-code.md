# 筆順データはアプリのコードと別ファイルに置く

筆順は KanjiVG（CC BY-SA 3.0）から生成している。このライセンスには
「改変物は同じ条件で配布せよ」という継承条件があり、データとコードが
一体に見える置き方をすると、アプリのコードまで同じ条件に引きずられると
読まれかねない。そこで生成物は `assets/data/` に独立して置き、
ライセンス表示を同梱し、コードからは読み込むだけにしている。
ファイルを1つにまとめたくなっても、この分離は崩さない。
